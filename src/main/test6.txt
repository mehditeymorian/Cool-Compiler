class Main {

    main() : int {
        let a: real;
        let y: real;

        a <- 1.0;
        y <- 2.0;

        a++;

        y <- (a++ + ((a++ + a) * a++));

        out_string("a is: ");
        out_int( (int) a);
        print_line();
        out_string("y is: ");
        out_int( (int) y);


        return 0;
    }
}
