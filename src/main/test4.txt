class Main {

    main() : int {
        let x:int[];
        let size: int;
        let i: int;
        size <- input_int();

        x <- new Array(int,size);

        /*
        for(i <- 0; i < size; i+=1)
            x[i] <- i + 1;
        rof

        for(i <- 0; i < size*2; i+=1)
            out_int(x[i]);
        rof
           */
        out_str("second for loop");

        x <- new Array(int, size*2);

        for(i <- 0; i < size*2; i+=1)
            x[i] <- i + 100;
        rof

        for(i <- 0; i < size*2; i+=1)
            out_int(x[i]);
            print_line();
        rof



        //out_int(x[1]);



        return 0;
    }
}
